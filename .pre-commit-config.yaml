fail_fast: true
repos:
  - repo: local
    hooks:
      - id: biome
        name: Biome
        entry: docker compose run -T --rm worker npm run biome-check
        language: system
        always_run: true
        pass_filenames: false
